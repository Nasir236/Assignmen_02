import 'dart:math';

void main() {
  /* Q.9: Given a list of integers, write a dart code that returns the maximum value 
    from the list. */
  var myList = [10, 30, 20, 40, 50, 70, 60];
  print(myList);
  // Find maximum Number
  var maxNumber = max(10, 70);
  print(maxNumber);
}
